---
name: deployment
description: Deploy the application locally using Docker
license: MIT
compatibility: opencode
---

## What I do

- Build the complete project after checker using `docker compose build`
- Start the complete project using `docker compose up`
- Restart the application and rendering logic using `docker compose up app`

## When to use me

Use this after making changes to the application logic. 